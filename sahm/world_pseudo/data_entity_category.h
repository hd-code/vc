
#pragma once

namespace Data
{
    struct SEntityCategory
    {
        enum Enum
        {
            Character,
            Ground,
            Obstacle,
            NumberOfMembers,
            Undefined = -1,
        };
    };
}
