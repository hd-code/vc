
#pragma once

// namespace Data

struct SEntityCategory
{
    enum Enum
    {
        Character,
        Obstacle,
        Platform,
        NumberOfMembers,
        Undefined = -1;
    };
};